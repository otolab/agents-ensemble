---
"@agents-ensemble/core": patch
"@agents-ensemble/cli": patch
---

Improve operator-facing `permission.pending` representations for ACP payload variants and share the renderer through core between CLI output paths.
