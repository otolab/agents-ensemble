---
"@agents-ensemble/core": minor
"@agents-ensemble/cli": patch
---

Add the conductor `set_dispatch_hold` tool to batch trigger events while keeping operator and permission dispatch immediate, with TUI visibility in both layouts.
