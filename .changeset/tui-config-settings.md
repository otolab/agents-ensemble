---
"@agents-ensemble/core": patch
"@agents-ensemble/cli": patch
---

Add `tui.layout` and `tui.forceHyperlink` to `.ensemble/config.yaml`, resolved with the same env-over-config precedence as other Phase 1 settings.
