---
"@agents-ensemble/core": minor
"@agents-ensemble/cli": minor
---

worker ACP spawn を profile / CLI / 環境変数で切り替え可能にした。built-in preset（cursor / claude / codex）、custom command、resume 時の spawn 不一致検知を追加。
